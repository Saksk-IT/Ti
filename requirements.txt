# ============================================================================
# Flask 核心框架
# ============================================================================
Flask==3.1.2
Werkzeug==3.1.4
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.1.8
itsdangerous==2.2.0
blinker==1.9.0

# ============================================================================
# Flask 扩展
# ============================================================================
Flask-Limiter==3.11.0  # API 限流
limits==4.2  # Flask-Limiter 依赖
Flask-CORS==4.0.0  # CORS支持（用于小程序API）

# ============================================================================
# 数据验证和序列化
# ============================================================================
pydantic==2.5.0
email-validator==2.3.0  # Pydantic EmailStr 依赖
typing_extensions==4.15.0  # Pydantic 依赖

# ============================================================================
# 数据处理
# ============================================================================
pandas==2.3.3
numpy==2.0.2
openpyxl==3.1.5  # Excel 文件处理
et_xmlfile==2.0.0  # openpyxl 依赖
python-docx==0.8.11  # Word 文件处理

# ============================================================================
# 日期时间处理
# ============================================================================
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.3

# ============================================================================
# Markdown 和代码高亮
# ============================================================================
markdown-it-py==3.0.0
mdurl==0.1.2  # markdown-it-py 依赖
Pygments==2.19.2  # 代码高亮

# ============================================================================
# 工具库
# ============================================================================
python-dotenv==1.2.1  # 环境变量管理
rich==13.9.4  # 终端美化输出
colorama==0.4.6  # 跨平台颜色支持
six==1.17.0  # Python 2/3 兼容
packaging==24.2  # 版本处理
ordered-set==4.1.0  # 有序集合
wrapt==2.0.1  # 装饰器工具
Deprecated==1.3.1  # 废弃警告
importlib_metadata==8.7.1  # 元数据导入
zipp==3.23.0  # ZIP 文件处理
requests==2.31.0  # HTTP请求库（用于微信API）
PyJWT==2.8.0  # JWT token生成和验证
redis>=5.0.0,<6  # Redis 客户端（缓存/队列/限流共享存储）
rq>=1.16.0,<2  # 轻量任务队列（依赖 Redis）

# ============================================================================
# WSGI 服务器（生产环境）
# ============================================================================
gunicorn>=21.2.0
