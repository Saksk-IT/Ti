package fixture.invalid;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Isolated root used only to prove that the real Modulith verifier rejects illegal access. */
@SpringBootApplication
public class IllegalFixtureApplication {}
