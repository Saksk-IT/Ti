package io.saksk.ti.identity.api;

/**
 * Public identity module boundary.
 *
 * <p>Phase 2 intentionally declares no operation or DTO shape: those remain
 * {@code deferred_shape} until Phase 4 freezes observed authentication behavior.</p>
 */
public interface IdentityApplicationApi {
}
