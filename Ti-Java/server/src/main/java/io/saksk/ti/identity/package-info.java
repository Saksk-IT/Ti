@org.springframework.modulith.ApplicationModule(
        id = "identity",
        displayName = "Identity and Access",
        allowedDependencies = {"sharedkernel::api"})
package io.saksk.ti.identity;
